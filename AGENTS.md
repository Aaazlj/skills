# AGENTS.md

用于减少 AI 编码中的常见错误。与项目专属规范结合使用；遵循适用的指令优先级。

基本取舍：优先保证正确性与可验证性，避免不必要的复杂度。简单任务使用判断力，不机械增加流程。

## 1. 先理解，再编码

- 实现前明确关键假设，不隐藏不确定性。
- 存在多种会影响实现的解释时，说明差异，不默默选择。
- 关键需求不清楚时，指出具体疑点，先澄清再执行依赖该信息的步骤。
- 有更简单的方案时主动说明；必要时对不合理的复杂度提出异议。
- 修改前理解相关业务逻辑、调用链和现有实现，不凭局部症状直接下结论。

## 2. 简单优先

- 只实现明确要求的功能，不添加推测性的需求。
- 优先复用已有实现，不为单次使用创建抽象。
- 不添加未要求的灵活性、可配置性或未来扩展结构。
- 不为不可能发生的场景增加错误处理；保留真实边界所需的校验与错误处理。
- 能用 50 行解决的问题，不写成 200 行。
- 自检：资深工程师是否会认为这过度复杂？如果会，继续简化。

## 3. 精准修改

- 每一处改动都应能直接追溯到当前请求。
- 不顺手改进无关代码、注释或格式，不重构未出问题的部分。
- 遵循项目现有风格，即使个人偏好不同。
- 删除本次修改导致不再使用的导入、变量和函数。
- 发现原有的无关死代码时可以指出，但未经要求不删除。
- 编辑源代码优先使用 `apply_patch`，以保留 Codex 的文件变更记录。

## 4. 以可验证的结果为目标

将任务转化为明确的成功标准：

| 任务 | 验证方式 |
| --- | --- |
| 添加校验 | 为无效输入编写测试，并使其通过 |
| 修复缺陷 | 先用测试复现问题，再验证修复 |
| 重构实现 | 确保修改前后相关测试均通过 |

多步骤任务先给出简短计划，并为每一步指定验证方式：

```text
1. [步骤] → 验证：[检查方式]
2. [步骤] → 验证：[检查方式]
3. [步骤] → 验证：[检查方式]
```

- 持续执行与验证，直到满足成功标准。
- 验证范围与改动风险匹配，避免为简单任务引入不必要的流程。
- 无法完成验证时，明确说明未验证的内容及原因，不将其表述为已通过。

## 5. 主动并行协作

- 主动使用子代理，将任务拆分为尽可能多的、有实际价值且可独立执行的工作流。
- 为每个子代理定义明确的职责、范围和交付结果。
- 避免多个子代理修改同一文件；可独立执行的工作并行开展。
- 要求子代理返回简洁的发现、验证结果和遗留问题。
- 等待所有子代理完成后，合并发现、解决冲突，再给出最终答复。
- 无法有效拆分的简单任务直接执行，不为并行而制造工作。

## 6. 注释规范

- 每个导出函数必须有文档注释，使用对应语言的 docstring、JSDoc 等形式。
- 每个非平凡算法必须有解释性注释。
- 注释重点说明“为什么”，避免只复述代码“做了什么”。
- 不以追求简洁或可读性为由省略必要注释。
- 编辑已有代码时，以上要求应用于本次新增或修改的内容，不顺手补写无关代码的注释。

## 执行效果

有效执行这些规范应表现为：更少的无关改动、更少因过度设计导致的返工，以及在实现前澄清关键疑点，而不是出错后补问。
